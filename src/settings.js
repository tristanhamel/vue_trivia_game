export const STAGGERING_DELAY = 500; // for counter adding points and tiles illuminating after an answer
export const NEXT_QUESTION_DELAY = 700; // for moving on to next question after having added points
export const ANSWER_RESOLUTION_DELAY = 500; // anim question cards with right/wrong answer
export const TILES_STAGGER_DELAY = 80;
export const FALLING_TILES_INTERVAL = 100;